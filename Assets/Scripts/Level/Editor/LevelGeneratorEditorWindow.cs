using UnityEditor;
using Sirenix.OdinInspector;
using Sirenix.OdinInspector.Editor;
using UnityEngine;
using Unity.Plastic.Antlr3.Runtime.Tree;

public class LevelGeneratorEditorWindow : EditorWindow
{
    /*private object instance;
    private PropertyTree tree;

    [MenuItem("Generator Window/Level Editor Window")]
    public static void ShowWindow()
    {
        LevelGeneratorEditorWindow window = GetWindow<LevelGeneratorEditorWindow>();
        window.instance = new DungeonGenerator();
        window.tree = PropertyTree.Create(window.instance);
    }

    [InitializeOnLoadMethod]
    public static void ShowWindowOnStart()
    {
        EditorApplication.delayCall += () => {
            GetWindow<LevelGeneratorEditorWindow>("My Editor Window", true);
        };
    }

    void OnGUI()
    {
        tree.Draw(false);
    }*/
}
